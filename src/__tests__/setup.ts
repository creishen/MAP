/* 
  file summary: vitest test environment setup file.
  responsibilities: imports jest-dom matchers for vitest runtime assertions.
  role in system: loaded automatically by vitest via setupFiles configuration in vite.config.ts.
*/

import '@testing-library/jest-dom';
